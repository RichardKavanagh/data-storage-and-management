# Lab 5

HBase HDFS YCSB execution Lab

This lab swaps an installed instance of HBase to use HDFS instead of the local filesystem.
It then loads and runs a YCSB workload against the new HBase instance. 
